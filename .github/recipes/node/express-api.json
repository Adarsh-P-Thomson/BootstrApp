{
  "name": "Node.js Express API",
  "description": "Creates a basic Node.js Express API with a layered structure.",
  "flags": {
    "typescript": "--ts",
    "nodemon": "--nodemon",
    "cors": "--cors"
  },
  "commands": [
    {
      "command": "New-Item -ItemType Directory -Force -Path {{projectName}} ; cd {{projectName}} ; npm init -y ; npm install express ; New-Item -ItemType Directory -Force -Path src/routes, src/controllers, src/services",
      "description": "Initialize project, install Express, and create folder structure safely."
    },
    {
      "command": "Out-File -FilePath src/index.js -Encoding utf8 -InputObject 'const express = require(\"express\"); const app = express(); const port = 3000; app.get(\"/\", (req, res) => res.send(\"Hello World!\")); app.listen(port, () => console.log(`Example app listening on port ${port}!`));'",
      "description": "Create a basic server file."
    }
  ]
}
