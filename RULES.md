# BootstrApp Project Generation Rules

This document outlines the rules and conventions the BootstrApp agent follows when generating new projects.

## 1. Project Naming Conventions

- Project names should be in `kebab-case`.
- The agent will automatically convert any input into this format.

## 2. Project Location

- All new projects will be generated directly in the root of the workspace.

---
